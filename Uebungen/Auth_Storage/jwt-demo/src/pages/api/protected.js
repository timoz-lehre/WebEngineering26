import jwt from 'jsonwebtoken';

export default function handler(req, res) {
  // logic which checks if the auth header is valid
  // extract jwt from header
  // verify jwt
  // give access by sending message
  // error if no access not allowed
}